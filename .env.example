# === Stage 2 DevOps - Blue/Green with Nginx Upstreams ===
# The grader/CI will set these. These are safe defaults for local tests.

# REQUIRED: App images (no building in CI). Replace with provided images.
BLUE_IMAGE=<set-by-grader>
GREEN_IMAGE=<set-by-grader>

# Release identifiers (will be echoed by the app via X-Release-Id)
RELEASE_ID_BLUE=v1.0.0
RELEASE_ID_GREEN=v1.0.1

# Active pool: blue or green (controls which server is primary vs backup)
ACTIVE_POOL=blue

# Nginx public port
PORT=8080
